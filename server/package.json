{
  "name": "@cacathead/server",
  "private": true,
  "scripts": {
    "dev": "cross-env DEBUG=true pipenv run python manage.py runserver",
    "judge": "cross-env C_FORCE_ROOT=true NODE_NAME=local_dev pipenv run celery -A CaCatHead.core worker -l INFO"
  }
}